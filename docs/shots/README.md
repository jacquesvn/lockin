# Screenshots

The four PNGs the top of the root README points at. They are **real captures of a real
install** — never a mockup, never the design prototype. A screenshot in a README is a
promise the app has to keep.

| File | Screen | Take it when |
|---|---|---|
| `today.png` | Today | mid-programme, drills partly ticked, streak visible |
| `progress.png` | Progress | after enough sessions that the charts and a leak have something to say |
| `plan.png` | Practice → Plan | any time — it shows the twelve weeks and the keystone |
| `session.png` | a running guided session | mid-drill, so the timer and the drill card are both live |

Landscape, dark theme, no personal identifiers in view — crop the gamertag out if you set one.

Once all four are here, uncomment the image table at the top of the root `README.md`.
